import React, { Component } from 'react';
import './App.css';
import { Button } from '@mui/material';

class App extends Component {
  render() {
    return (
      <div>
        <h1>Bem-vindo ao Meu Projeto React</h1>
        <Button variant="contained"> button test</Button>
      </div>
    );
  }
}

export default App;
